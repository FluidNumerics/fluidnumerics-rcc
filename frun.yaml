tests:
  - command_group: "pytorch"
    execution_command: "test/pytorch/benchmark.sh"
    output_directory: "pytorch"
    partition: "a100"
    batch_options: "--time=1:00:00 --gres=gpu:1 --ntasks=1 --cpus-per-task=8"
